<nav class="sticky top-0 z-50 w-full bg-gray-900/80 backdrop-blur-md border-b border-gray-800 px-4 md:px-8 py-3">
    <div class="max-w-[1400px] mx-auto flex items-center justify-between gap-4">
      
      <!-- Hamburger Menu Button -->
      <button id="menu-btn" class="flex-shrink-0 focus:outline-none hover:opacity-75 transition-opacity">
      <div class="text-2xl md:text-3xl font-extrabold text-red-600 tracking-tighter">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M4 6H20M4 12H20M4 18H20" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
      </div>
      </button>

      <!-- Side Bar -->
      <!-- Sidebar Overlay/Backdrop -->
      <div id="sidebar-backdrop" class="fixed inset-0 bg-black bg-opacity-50 z-40 hidden"></div>

      <!-- Sidebar Content -->
      <div id="sidebar" class="fixed top-0 left-0 h-full w-64 bg-white shadow-2xl z-50 transform -translate-x-full transition-transform duration-300 ease-in-out">
      <div class="p-6 bg-pink-500 h-[100%]">
          <h2 class="text-2xl font-bold text-red-600">Menu</h2>
          <nav class="mt-8 space-y-4">
          <a href="#" class="block text-gray-800 hover:text-red-600">Home</a>
          <a href="#" class="block text-gray-800 hover:text-red-600">Dashboard</a>
          <a href="#" class="block text-gray-800 hover:text-red-600">Settings</a>
          </nav>
          <button id="close-btn" class="mt-10 text-sm text-gray-500 underline">Close Menu</button>
      </div>
      </div>


      
      <!-- 1. Left: Logo -->
      <div class="flex-shrink-0 hidden md:inline">
        <a href="/" class="text-2xl md:text-3xl font-extrabold text-red-600 tracking-tighter">
          MFLIX<span class="text-white">.</span>
        </a>
      </div>
      

        <!-- 2. Center: Search Bar -->
        <div class="flex-1 flex justify-center max-w-2xl">
            <form action="/search" method="post" class="group h-11 md:h-12 w-full border border-gray-700 bg-gray-800/50 p-1 rounded-xl flex justify-between items-center transition-all focus-within:border-red-600 focus-within:ring-1 focus-within:ring-red-600">
                
                <!-- Search Icon & Input -->
                <div class="flex items-center flex-1 px-3 gap-2 md:gap-3">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 md:h-5 md:w-5 text-gray-400 group-focus-within:text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                    </svg>
                    <input 
                        type="text" 
                        name="query"
                        autocomplete="off"
                        placeholder="Search..." 
                        class="w-full bg-transparent outline-none text-gray-100 placeholder-gray-500 text-xs md:text-sm font-medium"
                    >
                </div>

                <!-- Action Button -->
                <button type="submit" class="bg-red-600 hover:bg-red-700 active:scale-95 h-full px-4 md:px-6 rounded-lg text-white text-[10px] md:text-xs font-bold transition-all shadow-lg shadow-red-900/20 flex justify-center items-center">
                    SEARCH
                </button>
            </form>
        </div>

      <!-- 3. Right: Account -->
      <!-- Main Wrapper with 'group' for hover logic -->
      <div class="relative group flex-shrink-0">
          
          <!-- Account Trigger Button -->
          <div class="flex items-center gap-3 cursor-pointer py-2">
              <span class="hidden lg:inline text-sm font-semibold text-gray-400 group-hover:text-white transition-colors">
                  Account
              </span>
              <div class="h-9 w-9 md:h-10 md:w-10 rounded-full bg-gray-800 border border-gray-700 flex items-center justify-center group-hover:border-red-600 transition-all shadow-lg">
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-400 group-hover:text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                  </svg>
              </div>
          </div>

          <!-- Dropdown Menu -->
          <!-- 'hidden group-hover:block' ensures it only shows on hover -->
          <div id="control-login-menu" class="absolute right-0 top-full pt-2 w-48 hidden group-hover:block z-50 animate-in fade-in slide-in-from-top-2 duration-200">
              <div class="bg-gray-800 border border-gray-700 rounded-xl shadow-2xl overflow-hidden backdrop-blur-xl bg-opacity-95">
                  
                  <!-- Login Link -->
                  <a href="/login" class="flex items-center gap-3 px-4 py-3 text-sm font-medium text-gray-300 hover:bg-gray-700/50 hover:text-red-500 transition-all border-b border-gray-700/50">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1" /></svg>
                      Sign In
                  </a>

                  <!-- Sign Up Link -->
                  <a href="/signup" class="flex items-center gap-3 px-4 py-3 text-sm font-medium text-gray-300 hover:bg-gray-700/50 hover:text-white transition-all">
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z" /></svg>
                      Create Account
                  </a>
              </div>
          </div>
      </div>
    </div>
</nav>



<script>
    const menuBtn = document.getElementById('menu-btn');
    const closeBtn = document.getElementById('close-btn');
    const sidebar = document.getElementById('sidebar');
    const backdrop = document.getElementById('sidebar-backdrop');
    function toggleSidebar() {
        // Slid the sidebar in/out
        sidebar.classList.toggle('-translate-x-full');
        // Show/hide the dark overlay
        backdrop.classList.toggle('hidden');
    }
    menuBtn.addEventListener('click', toggleSidebar);
    closeBtn.addEventListener('click', toggleSidebar);
    backdrop.addEventListener('click', toggleSidebar); // Close when clicking outside
</script>